servicecomb:
  registry:
    address:  # If type is File then address will be the path of the file
  protocols:
    rest:
      listenAddress: "127.0.0.1:8081"
  handler:
    chain:
      Provider:
        default: tracing-provider,bizkeeper-provider